<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>at.ecotrack</groupId>
        <artifactId>ecotrack-server</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>module-userprofile</artifactId>
    <name>EcoTrack UserProfile Module</name>
    <description>Supporting Domain: EcoUser-Profile, Levels, Milestones (CRUD Architecture)</description>

    <dependencies>
        <dependency>
            <groupId>at.ecotrack</groupId>
            <artifactId>shared-kernel</artifactId>
        </dependency>
        <!-- Dependency auf Scoring-Modul für Events -->
        <dependency>
            <groupId>at.ecotrack</groupId>
            <artifactId>module-scoring</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
